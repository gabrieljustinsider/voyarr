# Compile/Download-heavy deps installed in their own Docker layer so they
# stay cached independently of app dependency changes.
dlib==20.0.1
face-recognition==1.3.0
opencv-python-headless
scikit-learn
