{
  "name": "voyarr-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'no build needed'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
